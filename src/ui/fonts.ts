import { Inter } from "next/font/google";
import { Lusitana } from "next/font/google";

export const inter = Inter({subsets: ['latin'], weight: ['300', '500', '700']})
export const lusitana = Lusitana({subsets: ['latin'], weight: ['400', '700']});